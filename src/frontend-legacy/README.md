# Frontend Legacy

AngularJS 1.8.3 single-page application. No build step required — dependencies are loaded via CDN.

## Prerequisites

- [Node.js](https://nodejs.org/) (for `npx`)

## Run locally

From this directory:

```bash
npx serve .
```

Then open http://localhost:3000 in your browser.
